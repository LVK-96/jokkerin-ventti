{
  "name": "jokkerin-ventti",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "npm run build:wasm && vite",
    "dev:watch": "concurrently -k \"npm run watch:wasm\" \"vite\"",
    "watch:wasm": "cd wasm && cargo watch -s 'RUSTFLAGS=\"-C target-feature=+relaxed-simd\" rustup run nightly wasm-pack build --target web --out-dir pkg --features portable_simd'",
    "build": "npm run build:wasm && tsc && vite build",
    "build:wasm": "cd wasm && RUSTFLAGS=\"-C target-feature=+relaxed-simd\" rustup run nightly wasm-pack build --target web --out-dir pkg --features portable_simd",
    "preview": "vite preview"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "typescript": "latest",
    "vite": "latest",
    "vite-plugin-wasm-pack": "^0.1.12"
  },
  "engines": {
    "node": ">=20"
  }
}